# config/packages/test/doctrine.yaml
doctrine:
    dbal:
        driver: 'pdo_mysql'
        server_version: '5.7' # Ajustez en fonction de votre version de MySQL
        url: '%env(resolve:DATABASE_URL_TEST)%'
